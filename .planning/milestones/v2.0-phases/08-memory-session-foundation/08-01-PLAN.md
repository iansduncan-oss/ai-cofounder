---
phase: 08-memory-session-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/agent-server/src/services/conversation-ingestion.ts
  - apps/agent-server/src/routes/agents.ts
  - apps/agent-server/src/agents/tool-executor.ts
  - packages/db/src/repositories.ts
  - apps/agent-server/src/__tests__/conversation-ingestion.test.ts
autonomous: true
requirements: [MEM-01, MEM-03]

must_haves:
  truths:
    - "Every agent response triggers a conversation summary ingestion into RAG within 30s"
    - "Conversations shorter than 30 messages still get summarized and ingested"
    - "After git_clone succeeds, project documentation is auto-ingested into RAG"
  artifacts:
    - path: "apps/agent-server/src/services/conversation-ingestion.ts"
      provides: "ConversationIngestionService with eager summarization + enqueue"
      exports: ["ConversationIngestionService"]
    - path: "apps/agent-server/src/__tests__/conversation-ingestion.test.ts"
      provides: "Tests covering eager summarization and RAG ingestion trigger"
      min_lines: 80
  key_links:
    - from: "apps/agent-server/src/routes/agents.ts"
      to: "apps/agent-server/src/services/conversation-ingestion.ts"
      via: "fire-and-forget call after agent response"
      pattern: "conversationIngestion\\.ingestAfterResponse"
    - from: "apps/agent-server/src/agents/tool-executor.ts"
      to: "packages/queue/src/queues.ts"
      via: "enqueueRagIngestion after git_clone success"
      pattern: "enqueueRagIngestion.*ingest_repo"
---

<objective>
Build the conversation auto-ingestion pipeline and project docs trigger so every conversation and workspace registration feeds into the RAG vector store automatically.

Purpose: MEM-01 requires conversations ingested within 30s of completion. MEM-03 requires project docs ingested on registration. Both are data pipeline tasks that feed downstream session context (Plan 03).
Output: ConversationIngestionService, updated agents.ts routes, updated git_clone tool handler, tests.
</objective>

<execution_context>
@/Users/ianduncan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ianduncan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v2.0-phases/08-memory-session-foundation/08-RESEARCH.md
@.planning/ROADMAP.md
@apps/agent-server/src/routes/agents.ts
@apps/agent-server/src/plugins/queue.ts
@apps/agent-server/src/agents/tool-executor.ts
@apps/agent-server/src/agents/summarizer.ts
@packages/db/src/repositories.ts
@packages/queue/src/queues.ts
@packages/queue/src/scheduler.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ConversationIngestionService and wire into agent routes</name>
  <files>
    apps/agent-server/src/services/conversation-ingestion.ts
    apps/agent-server/src/routes/agents.ts
    packages/db/src/repositories.ts
  </files>
  <action>
    Create `apps/agent-server/src/services/conversation-ingestion.ts` with a `ConversationIngestionService` class:

    Constructor takes: `db: Db`, `llmRegistry: LlmRegistry`, `embeddingService?: EmbeddingService`.

    Method `ingestAfterResponse(conversationId: string, userMessage: string, agentResponse: string)`:
    1. Fire-and-forget — wrap entire body in try/catch, log errors as warn (non-fatal).
    2. Call `getConversationMessageCount(db, conversationId)` to get total message count.
    3. If message count <= 30 (below the existing lazy summarization threshold), call `summarizeMessages(llmRegistry, [{role:"user", content: userMessage}, {role:"assistant", content: agentResponse}])` to create an eager short summary for RAG indexing. Save via `saveConversationSummary(db, { conversationId, summary, messageCount, fromMessageCreatedAt: new Date(), toMessageCreatedAt: new Date() })`.
    4. After ensuring a summary exists (either created in step 3 or pre-existing from the lazy path), enqueue `enqueueRagIngestion({ action: "ingest_conversations", sourceId: conversationId })`.
    5. This replaces the existing per-turn `ingest_text` fire-and-forget in both the `/run` and `/run/stream` handlers.

    In `apps/agent-server/src/routes/agents.ts`:
    - Import `ConversationIngestionService`.
    - Create a single instance at the top of `agentRoutes` using `app.db`, `app.llmRegistry`, `app.embeddingService`.
    - In the `/run` handler (around line 222-230), replace the existing `enqueueRagIngestion({ action: "ingest_text", ... })` block with `conversationIngestion.ingestAfterResponse(result.conversationId, message, result.response)`. Keep it fire-and-forget (no await, .catch(() => {})).
    - In the `/run/stream` handler (around line 377-383), do the same replacement.
    - Keep the `const redisEnabled = !!optionalEnv("REDIS_URL", "")` guard — only call ingestAfterResponse when redisEnabled and embeddingService are available.

    In `packages/db/src/repositories.ts`:
    - Add new function `getRecentSessionSummaries(db: Db, userId: string, limit = 3)` that:
      1. Selects recent conversations for the user: `db.select({ id: conversations.id }).from(conversations).where(eq(conversations.userId, userId)).orderBy(desc(conversations.updatedAt)).limit(10)`.
      2. If none, returns [].
      3. Gets latest summary for each conversation: `db.select({ conversationId, summary, createdAt }).from(conversationSummaries).where(inArray(conversationSummaries.conversationId, convIds)).orderBy(desc(conversationSummaries.createdAt)).limit(limit)`.
    - Export `getRecentSessionSummaries` from both repositories.ts and the package index.
    - Build `@ai-cofounder/db` after changes: `npm run build -w @ai-cofounder/db`.

    IMPORTANT: Do not use the existing `getRecentConversationSummaries(db, since)` — it filters by date, not by user. The new function filters by userId via the conversations join.
  </action>
  <verify>
    <automated>npm run build -w @ai-cofounder/db && npm run test -w @ai-cofounder/agent-server -- --run -t "conversation ingestion"</automated>
    <manual>Verify ConversationIngestionService imports compile and agents.ts routes reference it</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>
    - ConversationIngestionService exists with ingestAfterResponse method
    - Both /run and /run/stream routes call ingestAfterResponse instead of raw ingest_text
    - getRecentSessionSummaries exists in repositories.ts and is exported
    - Conversations of any length get summarized and enqueued for RAG ingestion
    - @ai-cofounder/db builds cleanly
  </done>
</task>

<task type="auto">
  <name>Task 2: Add project docs auto-ingestion on git_clone and write tests</name>
  <files>
    apps/agent-server/src/agents/tool-executor.ts
    apps/agent-server/src/__tests__/conversation-ingestion.test.ts
  </files>
  <action>
    In `apps/agent-server/src/agents/tool-executor.ts`, in the `case "git_clone"` block (line ~413-418):
    - After `const result = await workspaceService.gitClone(...)`, add a fire-and-forget RAG ingestion trigger:
    ```typescript
    // Auto-ingest project documentation on workspace registration (MEM-03)
    try {
      const { enqueueRagIngestion } = await import("@ai-cofounder/queue");
      const dirName = input.directory_name ?? input.repo_url.split("/").pop()?.replace(".git", "") ?? "repo";
      enqueueRagIngestion({
        action: "ingest_repo",
        sourceId: dirName,
      }).catch(() => {}); // fire-and-forget
    } catch { /* non-fatal */ }
    ```
    - This uses the existing `ingest_repo` worker handler in `plugins/queue.ts` which already reads files via workspaceService and ingests them.

    Create `apps/agent-server/src/__tests__/conversation-ingestion.test.ts`:
    - Follow the project mock pattern (vi.mock @ai-cofounder/shared, @ai-cofounder/db with mockDbModule spread, @ai-cofounder/llm with MockLlmRegistry, @ai-cofounder/queue, @ai-cofounder/rag).
    - Mock `enqueueRagIngestion` as vi.fn().mockResolvedValue(undefined).
    - Mock `getConversationMessageCount` returning configurable values.
    - Mock `summarizeMessages` returning "Test summary".
    - Mock `saveConversationSummary` returning { id: "summary-1" }.
    - Mock `getLatestConversationSummary` returning null (for short conversations) or a summary object.

    Tests for ConversationIngestionService:
    1. "ingestAfterResponse creates eager summary for short conversations (< 30 messages)" — mock messageCount=5, verify summarizeMessages called, saveConversationSummary called, enqueueRagIngestion called with action:"ingest_conversations".
    2. "ingestAfterResponse skips eager summary for long conversations (>= 30 messages)" — mock messageCount=35, verify summarizeMessages NOT called (lazy path handles it), enqueueRagIngestion still called.
    3. "ingestAfterResponse is non-fatal on errors" — mock getConversationMessageCount to throw, verify no exception propagates.
    4. "git_clone triggers ingest_repo enqueue" — test the tool-executor git_clone case by importing executeTool and calling with git_clone input, verify enqueueRagIngestion called with action:"ingest_repo".

    Use `describe("conversation ingestion", ...)` and `describe("project ingestion", ...)` blocks so the -t filter in VALIDATION.md works.
  </action>
  <verify>
    <automated>npm run test -w @ai-cofounder/agent-server -- --run -t "conversation ingestion|project ingestion"</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>
    - git_clone tool handler enqueues ingest_repo after successful clone
    - 4+ tests covering conversation ingestion and project ingestion pass
    - All existing tests still pass (no regressions)
  </done>
</task>

</tasks>

<verification>
- `npm run test -w @ai-cofounder/agent-server -- --run` passes (full agent-server suite)
- `npm run build -w @ai-cofounder/db` succeeds (new export compiles)
- New tests cover MEM-01 (eager summarization + enqueue) and MEM-03 (git_clone trigger)
</verification>

<success_criteria>
- Every agent response (both /run and /run/stream) triggers conversation summary ingestion via ConversationIngestionService
- Short conversations (< 30 messages) get eagerly summarized for RAG
- git_clone success auto-enqueues ingest_repo for project documentation
- getRecentSessionSummaries function available in @ai-cofounder/db for Plan 03
- All tests green
</success_criteria>

<output>
After completion, create `.planning/milestones/v2.0-phases/08-memory-session-foundation/08-01-SUMMARY.md`
</output>
