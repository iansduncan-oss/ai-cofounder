---
phase: 04-tests-quick-wins
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/agent-server/src/__tests__/e2e-goal-lifecycle.test.ts
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03, TEST-04, TEST-05, TEST-06]

must_haves:
  truths:
    - "E2E test creates a goal via POST /api/agents/run against a real test database and verifies rows exist"
    - "E2E test runs TaskDispatcher.runGoal() with MockLlmRegistry scripted responses and goal reaches completed status in DB"
    - "Test database is truncated between test runs so tests are isolated"
    - "E2E test runs in CI alongside existing unit tests without requiring Redis or external services"
  artifacts:
    - path: "apps/agent-server/src/__tests__/e2e-goal-lifecycle.test.ts"
      provides: "Full E2E goal lifecycle test with real DB"
      min_lines: 100
  key_links:
    - from: "apps/agent-server/src/__tests__/e2e-goal-lifecycle.test.ts"
      to: "apps/agent-server/src/server.ts"
      via: "buildServer() with real DB connection via DATABASE_URL env"
      pattern: "buildServer"
    - from: "apps/agent-server/src/__tests__/e2e-goal-lifecycle.test.ts"
      to: "apps/agent-server/src/agents/dispatcher.ts"
      via: "TaskDispatcher.runGoal() called directly with real DB"
      pattern: "TaskDispatcher.*runGoal"
    - from: "apps/agent-server/src/__tests__/e2e-goal-lifecycle.test.ts"
      to: "@ai-cofounder/db"
      via: "createDb() for real test database connection + truncation"
      pattern: "createDb|TRUNCATE"
---

<objective>
Create the E2E goal lifecycle integration test that exercises the full create-dispatch-complete flow against a real PostgreSQL test database with mocked LLM responses.

Purpose: TEST-01 through TEST-06 require a true end-to-end test that writes real rows to PostgreSQL, runs the TaskDispatcher with MockLlmRegistry, and verifies goal completion in the database. This is the only genuinely new ground in Phase 4 -- all Quick Win features are already implemented.

Output: `apps/agent-server/src/__tests__/e2e-goal-lifecycle.test.ts` -- a self-contained E2E test file with truncation helper, real DB wiring, and scripted LLM sequence.
</objective>

<execution_context>
@/Users/ianduncan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ianduncan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-tests-quick-wins/04-RESEARCH.md

@apps/agent-server/src/__tests__/e2e-execution.test.ts (existing mock-based E2E -- DO NOT copy its DB mock pattern)
@apps/agent-server/src/__tests__/conversation-routes.test.ts (mock pattern reference for build boilerplate)
@apps/agent-server/src/agents/dispatcher.ts (TaskDispatcher.runGoal API)
@apps/agent-server/src/server.ts (buildServer, dbPlugin, swagger registration)
@packages/db/src/client.ts (createDb function)
@packages/test-utils/src/mocks/llm.ts (MockLlmRegistry, toolUseResponse, textResponse)
@.github/workflows/ci.yml (CI PostgreSQL service config)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create E2E goal lifecycle test with real DB, truncation, and scripted LLM</name>
  <files>apps/agent-server/src/__tests__/e2e-goal-lifecycle.test.ts</files>
  <action>
Create `apps/agent-server/src/__tests__/e2e-goal-lifecycle.test.ts` with the following structure:

**Environment setup (beforeAll, BEFORE any imports):**
- Set `process.env.ANTHROPIC_API_KEY = "test-key-not-real"`
- Set `process.env.DATABASE_URL` to `process.env.TEST_DATABASE_URL ?? "postgresql://ci:ci@localhost:5432/ai_cofounder_test"`
- Set `process.env.BRIEFING_HOUR = "25"` (prevents scheduler from firing)
- DO NOT mock `@ai-cofounder/db` -- E2E tests require real DB writes

**Mocking strategy:**
- DO mock `@ai-cofounder/queue` (no Redis needed): `getRedisConnection`, `startWorkers`, `stopWorkers`, `closeAllQueues`, `setupRecurringJobs`, `enqueueAgentTask`, `enqueueReflection` -- all as vi.fn() with appropriate return values
- DO mock `@ai-cofounder/llm` with a class-based MockLlmRegistry that exposes a `mockComplete` vi.fn() so test cases can script responses via `mockResolvedValueOnce`
- Use dynamic `await import("../server.js")` and `await import("@ai-cofounder/db")` AFTER env vars are set and mocks are in place

**Truncation helper (inline in the file):**
- `truncateTestDb(db)` function that runs `db.execute(sql.raw('TRUNCATE TABLE tool_executions, llm_usage, code_executions, approvals, tasks, goals, conversation_summaries, messages, conversations, schedules, events, work_sessions, memories, n8n_workflows, prompts, reflections, document_chunks, ingestion_state, provider_health, personas, admin_users, channel_conversations, milestones, users CASCADE'))`
- Import `sql` from `drizzle-orm`

**Test lifecycle hooks:**
- `beforeAll`: create `db` via `createDb(process.env.DATABASE_URL!)` for direct DB queries
- `beforeEach`: call `vi.clearAllMocks()` then `truncateTestDb(db)`
- `afterAll`: call `truncateTestDb(db)` for cleanup

**Test: "creates goal via POST /api/agents/run and verifies DB rows":**
1. Script mockComplete sequence:
   - First call: return `toolUseResponse("create_plan", { goal_title: "E2E Test Goal", goal_description: "Test the lifecycle", goal_priority: "low", tasks: [{ title: "Research task", description: "Do research", assigned_agent: "researcher" }] })`
   - Second call: return `textResponse("Plan created. Tasks queued.")`
2. Build server via `const { app } = buildServer()`
3. Call `app.inject({ method: "POST", url: "/api/agents/run", payload: { message: "Build a test feature", userId: "e2e-test-user" } })`
4. Assert response status is 200
5. Extract `plan.goalId` from response JSON
6. Import `getGoal` and `listTasksByGoal` from `@ai-cofounder/db`
7. Verify `getGoal(db, goalId)` returns a goal with status "active"
8. Verify `listTasksByGoal(db, goalId)` returns at least 1 task
9. `await app.close()`

NOTE: The `toolUseResponse` and `textResponse` helpers should be defined inline in this file as helper functions that return the expected LLM response shape: `{ content: [{ type, ...}], model, stop_reason, usage: { inputTokens, outputTokens }, provider }`. Reference the shape used in `e2e-execution.test.ts` and `conversation-routes.test.ts`. Do NOT import from `@ai-cofounder/test-utils` since the mock pattern for `@ai-cofounder/llm` replaces the real module.

**Test: "dispatches goal tasks to completion via TaskDispatcher.runGoal()":**
1. Script mockComplete for initial plan creation (same as above)
2. Script additional mockComplete calls for specialist agent task execution:
   - Third call: `textResponse("Research complete. Found relevant information.")`
3. Build server, call POST /api/agents/run, extract goalId
4. Import `TaskDispatcher` from `../agents/dispatcher.js`
5. Create `new TaskDispatcher(app.llmRegistry, db)` -- pass the real test DB
6. Call `await dispatcher.runGoal(goalId)`
7. Verify the returned result has `status: "completed"` or similar
8. Import `getGoal` from `@ai-cofounder/db` and verify `getGoal(db, goalId)` now shows status "completed"
9. `await app.close()`

**Test: "database is clean between test runs (truncation works)":**
1. This test runs after the above tests (Vitest runs in order within a describe)
2. Import `listGoalsByConversation` or count goals
3. Verify there are zero goals in the DB (proves truncation ran)

**Important implementation notes:**
- The `TaskDispatcher` constructor signature is `constructor(registry: LlmRegistry, db: any)` -- check the actual constructor in `dispatcher.ts` before writing. It may also need additional services (workspaceService, etc.) -- if so, pass null/undefined for optional ones.
- If `buildServer()` creates timers (scheduler, health flush), `app.close()` MUST be called to prevent test hangs.
- The lifecycle test is complex -- if `TaskDispatcher.runGoal()` requires services not available in test (e.g., workspaceService), the test should verify up to the point of goal creation and task creation in DB, then separately test that tasks exist with correct structure. Adjust scope based on what's actually callable.
- If `runGoal` needs more mock responses than expected (e.g., for self-improvement analysis), add additional `mockComplete.mockResolvedValueOnce(textResponse(...))` calls.
  </action>
  <verify>
    <automated>cd /Users/ianduncan/Projects/ai-cofounder && npx vitest run apps/agent-server/src/__tests__/e2e-goal-lifecycle.test.ts --reporter=verbose 2>&1 | tail -30</automated>
    <manual>All tests in the E2E file pass. If tests fail due to no database connection (expected locally without test DB), verify the test file structure is correct and would pass in CI.</manual>
    <sampling_rate>run after this task commits, before plan completion</sampling_rate>
  </verify>
  <done>
    - E2E test file exists at `apps/agent-server/src/__tests__/e2e-goal-lifecycle.test.ts`
    - Test does NOT mock `@ai-cofounder/db` (uses real DB via `DATABASE_URL` env)
    - Truncation helper clears all tables between tests with CASCADE
    - At least 2 test cases: goal creation with DB verification, and either full lifecycle dispatch or DB cleanup verification
    - Tests pass when a PostgreSQL test database is available (CI environment)
    - `npm run test -w @ai-cofounder/agent-server` still passes (no regressions in other test files)
  </done>
</task>

</tasks>

<verification>
- `npx vitest run apps/agent-server/src/__tests__/e2e-goal-lifecycle.test.ts --reporter=verbose` -- all tests pass (requires test DB)
- `npm run test -w @ai-cofounder/agent-server` -- full agent-server test suite passes (no regressions)
- E2E test file does NOT contain `vi.mock("@ai-cofounder/db"` -- grep confirms real DB usage
</verification>

<success_criteria>
- E2E goal lifecycle test exercises POST /api/agents/run with real PostgreSQL writes
- Test database is truncated between runs via TRUNCATE ... CASCADE
- MockLlmRegistry provides scripted deterministic responses
- All tests pass in CI with the PostgreSQL service container
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/04-tests-quick-wins/04-01-SUMMARY.md`
</output>
