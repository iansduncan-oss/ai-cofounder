---
phase: 10-autonomous-execution-engine
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - apps/agent-server/src/routes/autonomous.ts
  - apps/agent-server/src/plugins/jwt-guard.ts
  - apps/agent-server/src/autonomous-session.ts
  - apps/agent-server/src/server.ts
  - apps/agent-server/src/__tests__/autonomous-routes.test.ts
  - packages/api-client/src/client.ts
  - packages/api-client/src/types.ts
autonomous: true
requirements: [TERM-01, TERM-05]

must_haves:
  truths:
    - "GET /api/autonomous/backlog returns goals ready for autonomous execution"
    - "GET /api/autonomous/sessions returns recent work sessions with enriched actionsTaken"
    - "POST /api/autonomous/:goalId/run triggers autonomous execution via BullMQ (202 response)"
    - "runAutonomousSession() uses listGoalBacklog() for deterministic goal pickup instead of LLM guessing"
    - "SSE stream at GET /api/goals/:id/execute/stream receives events from autonomous execution"
  artifacts:
    - path: "apps/agent-server/src/routes/autonomous.ts"
      provides: "REST endpoints for backlog, sessions, and run trigger"
      exports: ["autonomousRoutes"]
      min_lines: 60
    - path: "apps/agent-server/src/__tests__/autonomous-routes.test.ts"
      provides: "Route integration tests for autonomous API"
      min_lines: 80
    - path: "apps/agent-server/src/autonomous-session.ts"
      provides: "Updated runAutonomousSession with backlog-driven goal pickup"
      contains: "listGoalBacklog"
  key_links:
    - from: "apps/agent-server/src/routes/autonomous.ts"
      to: "packages/db/src/repositories.ts"
      via: "import { listGoalBacklog, listRecentWorkSessions }"
      pattern: "listGoalBacklog|listRecentWorkSessions"
    - from: "apps/agent-server/src/routes/autonomous.ts"
      to: "packages/queue/src/helpers.ts"
      via: "enqueueAgentTask() for non-blocking execution"
      pattern: "enqueueAgentTask"
    - from: "apps/agent-server/src/autonomous-session.ts"
      to: "packages/db/src/repositories.ts"
      via: "import { listGoalBacklog }"
      pattern: "listGoalBacklog"
    - from: "apps/agent-server/src/autonomous-session.ts"
      to: "apps/agent-server/src/services/autonomous-executor.ts"
      via: "new AutonomousExecutorService()"
      pattern: "AutonomousExecutorService"
    - from: "packages/api-client/src/client.ts"
      to: "apps/agent-server/src/routes/autonomous.ts"
      via: "fetch calls to /api/autonomous/* endpoints"
      pattern: "autonomous"
---

<objective>
Wire the autonomous execution engine to HTTP endpoints and the autonomous session entry point. Create REST API for backlog listing, session history, and manual goal execution trigger. Update `runAutonomousSession()` to use `listGoalBacklog()` for deterministic goal pickup. Add ApiClient methods for dashboard consumption.

Purpose: Plan 01 built the engine; this plan makes it accessible via REST API and wires it into the autonomous session loop so goals get picked up automatically. Without this wiring, the engine cannot be triggered.

Output: 3 REST endpoints, updated autonomous session with backlog-driven pickup, ApiClient methods, route integration tests.
</objective>

<execution_context>
@/Users/ianduncan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ianduncan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-autonomous-execution-engine/10-RESEARCH.md
@.planning/phases/10-autonomous-execution-engine/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: REST API routes + ApiClient methods + jwt-guard wiring</name>
  <files>
    apps/agent-server/src/routes/autonomous.ts
    apps/agent-server/src/plugins/jwt-guard.ts
    apps/agent-server/src/server.ts
    packages/api-client/src/client.ts
    packages/api-client/src/types.ts
  </files>
  <action>
    **1. Create `apps/agent-server/src/routes/autonomous.ts`:**

    Export `autonomousRoutes` as a `FastifyPluginAsync`. Register three endpoints:

    **`GET /` (list backlog):**
    - Query param: `limit` (optional, default 5, max 20)
    - Calls `listGoalBacklog(app.db, limit)` from `@ai-cofounder/db`
    - Returns `{ data: GoalBacklogItem[], count: number }`
    - Tags: `["autonomous"]`

    **`GET /sessions` (list recent work sessions):**
    - Query param: `limit` (optional, default 10, max 50)
    - Calls `listRecentWorkSessions(app.db, limit)` from `@ai-cofounder/db`
    - Returns `{ data: WorkSession[], count: number }`
    - Tags: `["autonomous"]`

    **`POST /:goalId/run` (trigger autonomous execution):**
    - Path param: `goalId`
    - Body: `{ userId?: string, createPr?: boolean }` (optional)
    - Validates goal exists via `getGoal(app.db, goalId)`
    - Returns 404 if goal not found
    - Enqueues via `enqueueAgentTask({ goalId, prompt: goal.description ?? goal.title, userId, priority: "normal" })` from `@ai-cofounder/queue`
    - Stores `queueJobId` in goal metadata via `updateGoalMetadata(app.db, goalId, { queueJobId: jobId })`
    - Returns 202: `{ jobId, status: "queued", goalId }`
    - Follow the exact non-blocking 202 pattern from `apps/agent-server/src/routes/execution.ts` lines 47-67
    - Tags: `["autonomous"]`

    **2. Wire routes in `apps/agent-server/src/plugins/jwt-guard.ts`:**
    - Import `autonomousRoutes` from `../routes/autonomous.js`
    - Register under `/api/autonomous` prefix (follow the same pattern as other route registrations in jwt-guard.ts)

    **3. Wire in `apps/agent-server/src/server.ts`:**
    - If the routes are registered via jwt-guard.ts (which is the pattern), no server.ts changes needed
    - However, if `AutonomousExecutorService` needs to be created as an app decorator for the worker to use, add it in the `onReady` hook after dispatcher and workspaceService are available:
      ```
      const executor = new AutonomousExecutorService(dispatcher, workspaceService, app.db, app.llmRegistry);
      app.decorate("autonomousExecutor", executor);
      ```
    - Only add the decorator if the worker needs it — check how dispatcher is currently made available to the worker

    **4. Add ApiClient methods in `packages/api-client/src/client.ts`:**
    - `listGoalBacklog(limit?: number)` — GET `/api/autonomous?limit=${limit}`
    - `listAutonomousSessions(limit?: number)` — GET `/api/autonomous/sessions?limit=${limit}`
    - `triggerAutonomousRun(goalId: string, opts?: { userId?: string, createPr?: boolean })` — POST `/api/autonomous/${goalId}/run`
    - Follow existing ApiClient patterns for error handling and typed responses

    **5. Add types in `packages/api-client/src/types.ts`:**
    - `GoalBacklogItem` interface: `{ id, title, description, status, priority, createdAt, updatedAt, taskCount, pendingTaskCount }`
    - `AutonomousRunResponse` interface: `{ jobId: string, status: "queued", goalId: string }`
    - Build api-client after changes: `npm run build -w @ai-cofounder/api-client`
  </action>
  <verify>
    <automated>cd /Users/ianduncan/Projects/ai-cofounder && npm run build -w @ai-cofounder/api-client && npm run build -w @ai-cofounder/agent-server 2>&1 | tail -5</automated>
    <manual>Verify routes registered at /api/autonomous, ApiClient has 3 new methods</manual>
  </verify>
  <done>
    - GET /api/autonomous returns goal backlog
    - GET /api/autonomous/sessions returns recent work sessions
    - POST /api/autonomous/:goalId/run enqueues execution and returns 202
    - ApiClient has listGoalBacklog, listAutonomousSessions, triggerAutonomousRun methods
    - Types exported for GoalBacklogItem and AutonomousRunResponse
    - Routes registered in jwt-guard under /api/autonomous prefix
  </done>
</task>

<task type="auto">
  <name>Task 2: Autonomous session backlog wiring + route tests</name>
  <files>
    apps/agent-server/src/autonomous-session.ts
    apps/agent-server/src/__tests__/autonomous-routes.test.ts
  </files>
  <action>
    **1. Update `apps/agent-server/src/autonomous-session.ts`:**

    Add `listGoalBacklog` to the imports from `@ai-cofounder/db`.

    Modify `runAutonomousSession()` to add backlog-driven goal pickup BEFORE the freeform orchestrator fallback. After the daily token limit check and work session creation (around line 157), add:

    ```
    // Attempt deterministic goal pickup from backlog
    const backlog = await listGoalBacklog(db, 1);
    if (backlog.length > 0) {
      const topGoal = backlog[0];
      logger.info({ goalId: topGoal.id, goalTitle: topGoal.title }, "picked goal from backlog");

      // Import and use AutonomousExecutorService for deterministic execution
      const { AutonomousExecutorService } = await import("./services/autonomous-executor.js");
      const { TaskDispatcher } = await import("./agents/dispatcher.js");

      const dispatcher = new TaskDispatcher(registry, db, embeddingService, sandboxService, undefined, workspaceService);
      const executor = new AutonomousExecutorService(dispatcher, workspaceService, db, registry);

      try {
        const { progress, actions } = await executor.executeGoal({
          goalId: topGoal.id,
          userId: "system-autonomous",
          workSessionId: session.id,
          repoDir: workspaceService ? optionalEnv("WORKSPACE_DIR", "/tmp/ai-cofounder-workspace") : undefined,
          createPr: true,
          onProgress: async (event) => {
            // Publish to SSE via redisPubSub if available
          },
        });

        totalTokens = progress.tasks.reduce((sum, t) => sum + (t.tokensUsed ?? 0), 0);
        status = progress.status === "completed" ? "completed" : "failed";
        summary = `Executed goal "${topGoal.title}" — ${progress.completedTasks}/${progress.totalTasks} tasks completed`;

        await completeWorkSession(db, session.id, {
          tokensUsed: totalTokens,
          durationMs: Date.now() - startTime,
          actionsTaken: { actions, goalId: topGoal.id, goalTitle: topGoal.title },
          status,
          summary,
        });

        // ... webhook notification ...
        return { sessionId: session.id, status, summary, tokensUsed: totalTokens, durationMs: Date.now() - startTime };
      } catch (err) {
        // Fall through to existing error handling
      }
    }
    // ... existing freeform orchestrator code continues as fallback ...
    ```

    Key points:
    - Use dynamic `import()` for AutonomousExecutorService and TaskDispatcher to avoid circular dependency issues
    - The backlog path returns early on success — the existing orchestrator code is the fallback for when no backlog goals exist
    - Pass `workspaceService` through the existing function parameter — it is already accepted
    - Wrap the executor call in try/catch so failures fall through to existing error handling
    - The `onProgress` callback can be left as a no-op for now since the SSE streaming already works through the BullMQ worker path — the manual trigger goes through enqueueAgentTask which already publishes events

    **2. Create `apps/agent-server/src/__tests__/autonomous-routes.test.ts`:**

    Follow CLAUDE.md test mock patterns exactly. Mock `@ai-cofounder/shared`, `@ai-cofounder/db` (using `...mockDbModule()` spread), `@ai-cofounder/llm`, `@ai-cofounder/queue` (mock `enqueueAgentTask`).

    Use `buildServer()` + `app.inject()` pattern for route testing (see existing route tests like `autonomy-routes.test.ts` for the pattern).

    Test cases:
    - **GET /api/autonomous (backlog):**
      - returns empty array when no backlog goals
      - returns goals sorted by priority when backlog exists
      - respects limit query param
    - **GET /api/autonomous/sessions:**
      - returns empty array when no sessions
      - returns recent sessions with data
    - **POST /api/autonomous/:goalId/run:**
      - returns 404 when goal not found
      - returns 202 with jobId when goal exists and job enqueued
      - stores queueJobId in goal metadata

    Mock `listGoalBacklog`, `listRecentWorkSessions`, `getGoal`, `updateGoalMetadata`, `enqueueAgentTask` with appropriate return values. Use `mockResolvedValueOnce` for per-test overrides.
  </action>
  <verify>
    <automated>cd /Users/ianduncan/Projects/ai-cofounder && npm run test -w @ai-cofounder/agent-server -- --run -t "autonomous.*route|autonomous.*backlog|autonomous.*session" --reporter=verbose 2>&1 | tail -30</automated>
    <manual>Verify all route tests pass and autonomous-session.ts imports listGoalBacklog</manual>
  </verify>
  <done>
    - runAutonomousSession() calls listGoalBacklog() and picks top goal for execution (TERM-01)
    - Backlog-driven path uses AutonomousExecutorService for deterministic flow
    - Freeform orchestrator is fallback when no backlog goals exist
    - Work session completed with enriched actionsTaken including goal context (TERM-05)
    - 7+ route integration tests pass covering GET backlog, GET sessions, POST run
    - No regressions in existing test suite
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npm run build` — all packages build cleanly
2. `npm run test -w @ai-cofounder/agent-server -- --run --reporter=verbose` — all tests pass
3. `npm run test` — full suite green (except pre-existing failures)
4. Verify end-to-end flow conceptually: listGoalBacklog() -> AutonomousExecutorService.executeGoal() -> dispatcher.runGoal() -> gitCommit -> createPr -> completeWorkSession with structured actionsTaken
</verification>

<success_criteria>
- 3 REST endpoints functional at /api/autonomous (backlog, sessions, run)
- runAutonomousSession() deterministically picks goals from backlog
- ApiClient has methods for all 3 endpoints
- 7+ route tests pass
- Full autonomous execution flow is wired: backlog query -> executor -> dispatcher -> git -> PR -> work log
- SSE streaming works via existing infrastructure (no new SSE endpoints needed)
</success_criteria>

<output>
After completion, create `.planning/phases/10-autonomous-execution-engine/10-02-SUMMARY.md`
</output>
