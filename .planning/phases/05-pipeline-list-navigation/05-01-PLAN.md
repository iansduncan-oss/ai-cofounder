---
phase: 05-pipeline-list-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/dashboard/src/routes/pipelines.tsx
  - apps/dashboard/src/routes/pipeline-detail.tsx
  - apps/dashboard/src/routes/index.tsx
  - apps/dashboard/src/__tests__/pages/pipelines.test.tsx
autonomous: true
requirements: [NAV-01, NAV-02, LIST-01, LIST-02, LIST-03, LIST-04]

must_haves:
  truths:
    - "User can click Pipelines in the sidebar and see the pipeline list page"
    - "User can see all pipeline runs with state badge, stage count, and timing"
    - "User can filter pipeline list by state (waiting, active, completed, failed)"
    - "User can see the list auto-refresh every 10 seconds without manual action"
    - "User can click a pipeline row and navigate to /pipelines/:jobId"
  artifacts:
    - path: "apps/dashboard/src/routes/pipelines.tsx"
      provides: "Pipeline list page with filter and clickable rows"
      min_lines: 80
    - path: "apps/dashboard/src/routes/pipeline-detail.tsx"
      provides: "Pipeline detail route stub for navigation target"
      min_lines: 15
    - path: "apps/dashboard/src/routes/index.tsx"
      provides: "Router with pipelines/:jobId route registered"
      contains: "pipelines/:jobId"
    - path: "apps/dashboard/src/__tests__/pages/pipelines.test.tsx"
      provides: "Tests verifying list rendering, filtering, and navigation"
      min_lines: 60
  key_links:
    - from: "apps/dashboard/src/routes/pipelines.tsx"
      to: "apps/dashboard/src/api/queries.ts"
      via: "useListPipelines hook (already exists)"
      pattern: "useListPipelines"
    - from: "apps/dashboard/src/routes/pipelines.tsx"
      to: "/dashboard/pipelines/:jobId"
      via: "React Router Link component on each pipeline row"
      pattern: "Link.*to=.*pipelines/"
    - from: "apps/dashboard/src/routes/index.tsx"
      to: "apps/dashboard/src/routes/pipeline-detail.tsx"
      via: "lazy import and route path registration"
      pattern: "pipelines/:jobId"
---

<objective>
Rewrite the pipeline list page to display all pipeline runs with state, stage count, and timing; add a state filter; make rows clickable links to the detail route; and register the detail route in the router.

Purpose: This is Phase 5 of the Pipeline Dashboard UI milestone. It delivers the pipeline list as the primary entry point for pipeline monitoring, with filtering and navigation to individual pipeline runs.

Output: Rewritten pipelines.tsx, new pipeline-detail.tsx stub, updated router, and test file.
</objective>

<execution_context>
@/Users/ianduncan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ianduncan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/dashboard/src/api/queries.ts
@apps/dashboard/src/api/client.ts
@apps/dashboard/src/lib/query-keys.ts
@apps/dashboard/src/routes/index.tsx
@apps/dashboard/src/routes/goals.tsx
@apps/dashboard/src/components/layout/sidebar.tsx
@apps/dashboard/src/components/common/status-badge.tsx
@apps/dashboard/src/components/common/loading-skeleton.tsx
@apps/dashboard/src/components/common/empty-state.tsx
@apps/dashboard/src/components/layout/page-header.tsx
@apps/dashboard/src/components/ui/select.tsx
@apps/dashboard/src/components/ui/badge.tsx
@apps/dashboard/src/__tests__/test-utils.tsx
@apps/dashboard/src/__tests__/pages/overview.test.tsx
@packages/api-client/src/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite pipeline list page with state filter, timing, and clickable rows</name>
  <files>apps/dashboard/src/routes/pipelines.tsx</files>
  <action>
Rewrite `apps/dashboard/src/routes/pipelines.tsx` to replace the current expand/collapse card layout with a proper list page that has:

1. **Page header**: Keep "Pipelines" title and "Multi-stage agent pipeline runs" description. Keep the "Run Pipeline" button + SubmitPipelineDialog (preserve existing dialog code).

2. **State filter bar**: Add a `Select` dropdown above the list (matching the pattern in `goals.tsx`) with options: "All states", "Waiting", "Active", "Completed", "Failed". Use `useSearchParams` to persist the filter in the URL query string (key: `state`). Do NOT include "delayed" in the filter options (not in requirements).

3. **Pipeline list**: Replace the Card-based PipelineRow with a list of clickable rows using React Router `Link` to `/dashboard/pipelines/${run.jobId}`. Each row should display:
   - Pipeline ID (first 8 chars of `pipelineId`) as the primary label
   - State badge using the existing `PipelineStateBadge` component (keep this helper)
   - Stage count as text: "{stageCount} stages"
   - Timing: show `createdAt` as relative time via `RelativeTime` component. If `finishedAt` exists, show duration (finishedAt - createdAt) formatted as "Xm Ys" or "Xh Ym".
   - Goal ID (first 8 chars) as secondary info

   Style each row as: `className="block rounded-lg border bg-card p-4 transition-all hover:bg-accent hover:shadow-md hover:-translate-y-0.5"` (matching the goals.tsx row pattern).

4. **Client-side filtering**: Filter `runs` array by the selected state before rendering. Show filtered count if filter is active.

5. **Existing states**: Keep the existing loading (`ListSkeleton`), error, and empty states.

6. **Auto-refresh**: The `useListPipelines()` hook already has `refetchInterval: 10_000` — no changes needed. But add a small "Auto-refreshing" indicator text below the filter bar: `<p className="text-xs text-muted-foreground">Auto-refreshing every 10s</p>`.

7. **Imports**: Add `Link, useSearchParams` from `react-router`. Add `RelativeTime` from `@/components/common/relative-time`. Keep `Select` from `@/components/ui/select`. Remove `usePipeline` import (detail fetching moves to the detail page). Remove `ChevronDown`, `ChevronRight`, `Loader2` (used by old expand/collapse). Remove `StageProgress`, `StageIcon` helper components (they will be used in Phase 6 detail page — do NOT delete them entirely, instead move them to a new file `apps/dashboard/src/components/pipelines/stage-progress.tsx` so Phase 6 can import them).

8. **Duration helper**: Add a local helper function `formatDuration(startIso: string, endIso: string): string` that returns human-readable duration. Logic: `const ms = new Date(end).getTime() - new Date(start).getTime()`. If < 60s return `${Math.round(ms/1000)}s`. If < 3600s return `${Math.floor(ms/60000)}m ${Math.round((ms%60000)/1000)}s`. Else return `${Math.floor(ms/3600000)}h ${Math.floor((ms%3600000)/60000)}m`.

Keep the `PipelineStateBadge` and `stateConfig` helpers in this file — they are used here and can be exported if needed later. Remove the old `PipelineRow` component with expand/collapse behavior. Remove the `StageProgress` and `StageIcon` components from this file (move to separate file as noted above).
  </action>
  <verify>
    <automated>cd /Users/ianduncan/Projects/ai-cofounder && npx tsc --noEmit -p apps/dashboard/tsconfig.json 2>&1 | head -30</automated>
    <manual>Run `npm run dev -w @ai-cofounder/dashboard` and visit /dashboard/pipelines — verify filter dropdown appears, rows are clickable links</manual>
  </verify>
  <done>Pipeline list page shows all runs with state badge, stage count, timing, and clickable rows. State filter dropdown works. Auto-refresh indicator visible. StageProgress/StageIcon extracted to components/pipelines/stage-progress.tsx.</done>
</task>

<task type="auto">
  <name>Task 2: Create pipeline detail route stub and register in router</name>
  <files>apps/dashboard/src/routes/pipeline-detail.tsx, apps/dashboard/src/routes/index.tsx</files>
  <action>
1. **Create `apps/dashboard/src/routes/pipeline-detail.tsx`**: A minimal page that:
   - Uses `useParams()` from `react-router` to get `jobId`
   - Uses `usePageTitle("Pipeline Detail")` hook
   - Calls `usePipeline(jobId ?? null)` from `@/api/queries`
   - Renders a `PageHeader` with title "Pipeline {jobId first 8 chars}" and a "Back to Pipelines" link (use `Link` to `/dashboard/pipelines`)
   - Shows loading state if `isLoading`
   - Shows error state if `error`
   - When data loads, shows: pipeline state badge, stage count, created/finished times, and a placeholder message: "Detailed stage view coming in Phase 6"
   - Export as `PipelineDetailPage`

2. **Update `apps/dashboard/src/routes/index.tsx`**:
   - Add a lazy import for `PipelineDetailPage`: `const PipelineDetailPage = lazy(() => import("./pipeline-detail").then((m) => ({ default: m.PipelineDetailPage })));`
   - Add the route inside the dashboard children array, after the existing `pipelines` route: `{ path: "pipelines/:jobId", element: <PipelineDetailPage /> }`
  </action>
  <verify>
    <automated>cd /Users/ianduncan/Projects/ai-cofounder && npx tsc --noEmit -p apps/dashboard/tsconfig.json 2>&1 | head -30</automated>
    <manual>Navigate to /dashboard/pipelines/some-job-id — page renders with "Pipeline some-job" header and back link</manual>
  </verify>
  <done>Pipeline detail route is registered at /dashboard/pipelines/:jobId. Navigating to the URL shows the detail stub page with back navigation. URL updates correctly when clicking a pipeline row from the list.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests for pipeline list page and navigation</name>
  <files>apps/dashboard/src/__tests__/pages/pipelines.test.tsx</files>
  <action>
Create `apps/dashboard/src/__tests__/pages/pipelines.test.tsx` following the pattern in `overview.test.tsx`:

1. **Mock setup**: Mock `@/api/queries` with `useListPipelines` returning mock data. Mock `@/api/mutations` with `useSubmitGoalPipeline`. Use `vi.mock` and `vi.mocked` pattern.

2. **Mock data**: Create a `mockPipelineRuns` array with 3 entries covering different states:
   - One `completed` run with `finishedAt` set (to test duration display)
   - One `active` run with no `finishedAt` (to test running state)
   - One `failed` run

   Each mock entry shape: `{ jobId, pipelineId, goalId, stageCount, state, createdAt, finishedAt, failedReason, result }`

3. **Test cases** (at minimum):
   - `it("renders the Pipelines page title")` — checks "Pipelines" heading exists
   - `it("shows pipeline runs when loaded")` — checks that pipeline IDs (first 8 chars) appear in the document
   - `it("displays state badges for each run")` — checks "Completed", "Running", "Failed" badge text appears
   - `it("shows stage count for each run")` — checks "3 stages" text appears
   - `it("renders pipeline rows as links to detail page")` — use `screen.getAllByRole('link')` and check `href` contains `/dashboard/pipelines/` + jobId
   - `it("shows loading skeleton when loading")` — mock `isLoading: true`, check `.animate-pulse` elements exist
   - `it("shows error state on failure")` — mock `error: new Error("Network")`, check error message renders
   - `it("shows empty state when no runs")` — mock empty `runs: []`, check "No pipeline runs" text
   - `it("filters runs by state when filter is applied")` — use `renderWithProviders` with `initialEntries: ["/dashboard/pipelines?state=completed"]` and verify only the completed run renders (use `useSearchParams` mock pattern or pass initial URL)
   - `it("shows auto-refresh indicator")` — check "Auto-refreshing every 10s" text appears

4. **Use `renderWithProviders`** from `../test-utils` for all renders. Use `initialEntries: ["/dashboard/pipelines"]` to ensure router context is correct.

5. **Import pattern**: Import `PipelinesPage` from `@/routes/pipelines`. Import `screen` from `@testing-library/react`. Import `renderWithProviders` from `../test-utils`.

Note: The filter test needs the component to read `useSearchParams`. Since `renderWithProviders` wraps in `MemoryRouter`, pass `initialEntries: ["/dashboard/pipelines?state=completed"]` to simulate the filter being active.
  </action>
  <verify>
    <automated>cd /Users/ianduncan/Projects/ai-cofounder && npx vitest run apps/dashboard/src/__tests__/pages/pipelines.test.tsx --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>All pipeline list tests pass: renders title, shows runs with state/stages/timing, rows link to detail page, filter works, loading/error/empty states handled, auto-refresh indicator visible.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `npx tsc --noEmit -p apps/dashboard/tsconfig.json`
2. All pipeline tests pass: `npx vitest run apps/dashboard/src/__tests__/pages/pipelines.test.tsx`
3. All existing dashboard tests still pass: `npm test -w @ai-cofounder/dashboard`
4. Build succeeds: `npm run build -w @ai-cofounder/dashboard`
</verification>

<success_criteria>
- Sidebar "Pipelines" link navigates to the list page (NAV-01)
- Pipeline list shows all runs with state badge, stage count, and timing (LIST-01)
- State filter dropdown filters the displayed runs (LIST-02)
- List auto-refreshes every 10 seconds via refetchInterval (LIST-03)
- Clicking a pipeline row navigates to /dashboard/pipelines/:jobId (LIST-04)
- Detail route is registered and renders a stub page (NAV-02)
- All tests pass, TypeScript compiles, dashboard builds
</success_criteria>

<output>
After completion, create `.planning/phases/05-pipeline-list-navigation/05-01-SUMMARY.md`
</output>
