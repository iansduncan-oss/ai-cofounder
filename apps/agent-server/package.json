{
  "name": "@ai-cofounder/agent-server",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "echo 'TODO: configure lint'",
    "test": "echo 'TODO: configure test'",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@ai-cofounder/shared": "*",
    "@ai-cofounder/db": "*"
  }
}
