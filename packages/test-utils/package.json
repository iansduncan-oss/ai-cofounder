{
  "name": "@ai-cofounder/test-utils",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "echo 'test-utils: no build needed (source-only)'",
    "test": "vitest run",
    "clean": "echo 'nothing to clean'"
  },
  "peerDependencies": {
    "vitest": "^4.0.0"
  }
}
