# AdGuard + Unbound — environment variables
#
# Copy to .env in this directory.

# Tailscale IP of the VPS — DNS binds here only. Get with:
#   tailscale ip -4
# on the VPS, should look like 100.x.y.z
TAILSCALE_IP=100.64.0.0
